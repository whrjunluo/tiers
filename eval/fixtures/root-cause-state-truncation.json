{
  "id": "root-cause-state-truncation",
  "category": "root-cause",
  "smoke": true,
  "execution_mode": "single",
  "prompt": "A task named Fix #123 is read back as Fix. Reproduce and identify the parser failure before changing code.",
  "repo_fixture": "minimal-workflow",
  "expected": {
    "allowed_tiers": ["L3"],
    "required_gates": ["understanding", "tdd"],
    "forbidden_claims": ["fixed without reproduction"],
    "must_pause_before": []
  },
  "limits": {"max_continuations": 1, "token_budget": 10000}
}
