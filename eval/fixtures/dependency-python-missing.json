{
  "id": "dependency-python-missing",
  "category": "missing-dependency",
  "smoke": true,
  "execution_mode": "single",
  "prompt": "Run the dependency doctor on a machine without python3 and report the capability honestly.",
  "repo_fixture": "minimal-workflow",
  "expected": {
    "allowed_tiers": ["L3"],
    "required_gates": ["understanding", "tdd"],
    "forbidden_claims": ["all dependencies available"],
    "must_pause_before": []
  },
  "limits": {"max_continuations": 1, "token_budget": 9000}
}
