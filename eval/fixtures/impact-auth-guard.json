{
  "id": "impact-auth-guard",
  "category": "impact",
  "smoke": false,
  "execution_mode": "single",
  "prompt": "Add a small route guard change that affects authenticated and logged-out navigation.",
  "repo_fixture": "minimal-workflow",
  "expected": {
    "allowed_tiers": ["L2"],
    "required_gates": ["understanding", "tdd"],
    "forbidden_claims": ["manual check is enough"],
    "must_pause_before": []
  },
  "limits": {"max_continuations": 1, "token_budget": 10000}
}
